upstream frontend {
    server frontend:80;
}

server {
    listen 80;

    location / {
        proxy_pass http://frontend/;
        proxy_intercept_errors on;
        error_page 404 = /index.html;
    }
}